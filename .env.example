# =============================================================================
# Variables de entorno de ejemplo — copia esto a .env y ajusta los valores.
# El .env real nunca se sube al repositorio.
# =============================================================================

# URL del servidor NATS
NATS_URL=nats://localhost:4222

# Puertos de los servicios
PUBLISHER_PORT=8080
GATEWAY_PORT=8081
